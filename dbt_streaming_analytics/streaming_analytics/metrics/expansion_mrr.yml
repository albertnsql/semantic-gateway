metrics:
  - name: expansion_mrr
    description: MRR gained from plan upgrades.
    label: Expansion MRR
    type: simple
    type_params:
      measure: mrr_usd
    filter: |
      {{ Dimension('subscription__mrr_type') }} = 'expansion'
