metrics:
  - name: total_revenue
    description: Total revenue (calculated as the sum of successful payment transactions).
    label: Total Revenue
    type: simple
    type_params:
      measure: total_revenue_usd
