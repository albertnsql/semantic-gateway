fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
openai>=1.30.0
snowflake-connector-python>=3.10.0
PyYAML>=6.0.1
python-dotenv>=1.0.1
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
chromadb>=0.4.0
dbt-core>=1.8.0
dbt-snowflake>=1.8.0
# DuckDB is the active warehouse (warehouse_engine=duckdb) since the Snowflake
# trial expired; dbt-snowflake stays installed so DBT_TARGET=dev still works if
# that account is revived.
dbt-duckdb>=1.10.1
duckdb>=1.5.5
dbt-metricflow>=0.13.0
urllib3>=1.21.1,<2.0.0
# RSS sampling for the RAM log lines (core/memory.py). Optional at runtime — the
# module falls back to /proc/self/statm on Linux and GetProcessMemoryInfo on
# Windows, so a failed wheel build degrades the log line, not the gateway.
psutil>=5.9.0
# Orchestrates the diagnostic ("why") path — gateway/core/diagnostics/graph.py is the
# only module that imports it, so the rest of that package stays testable without it.
# Pinned to a minor range: node config injection is matched on the RAW annotation
# string, which is undocumented behaviour that could move (see graph.py's note).
# Costs 10.7s of import and +68.6MB RSS on first build, so the graph is built lazily
# behind `diagnostics_enabled` rather than in lifespan() — the instance already sits
# near 251MB of 512MB with the warm MetricFlow engine loaded.
# Deliberately NOT installing langchain-openai: the Google -> Groq -> OpenRouter
# fallback chain already exists in core/llm-adjacent code, and a second provider
# policy is how those drift apart.
langgraph>=1.2,<2.0
